name: Update flake.lock

on:
  schedule:
    - cron: "0 4 * * 1" # 04:00 UTC every Monday
  workflow_dispatch:

permissions:
  contents: write # Push the update branch.
  pull-requests: write # Open the update PR.

# Serialize a manual dispatch that overlaps the weekly schedule, so the two
# runs cannot open competing update PRs.
concurrency:
  group: flake-update
  cancel-in-progress: false

jobs:
  update:
    name: Update flake inputs
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Reuse the shared installer wiring (Cachix self-skips with no cache set),
      # keeping the nix-installer SHA pin in exactly one place.
      - uses: ./.github/actions/nix-setup

      # Open or refresh a PR for every flake input so the pinned inputs stay
      # aligned without manual edits.
      #
      # The default GITHUB_TOKEN does not trigger CI on the bot's PRs, which
      # prevents workflow loops. Re-run CI by pushing an empty commit or
      # closing and reopening the PR; use a PAT via `token:` to trigger CI.
      - name: Update flake.lock
        uses: DeterminateSystems/update-flake-lock@834c491b2ece4de0bbd00d85214bb5e83b4da5c6 # v28
        with:
          pr-title: "chore: update flake.lock"
          commit-msg: "chore: update flake.lock"
